<x-layouts::app :title="__('Teacher Dashboard')">
    <div class="space-y-6">
        {{-- Header --}}
        <div class="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
            <div>
                <flux:heading size="xl">{{ __('Welcome back, :name', ['name' => $teacher->name]) }}</flux:heading>
                @if ($currentSession && $currentTerm)
                    <flux:text class="mt-1">
                        {{ $currentSession->name }} &mdash; {{ $currentTerm->name }}
                    </flux:text>
                @else
                    <flux:text class="mt-1 text-amber-600 dark:text-amber-400">
                        {{ __('No active session/term set by the school admin.') }}
                    </flux:text>
                @endif
            </div>
        </div>

        {{-- ── Rejected Submissions Alert ─────────────────────────── --}}
        @if ($rejectedSubmissions->isNotEmpty())
            <section aria-label="{{ __('Rejected submissions alert') }}" class="rounded-lg border border-red-300 dark:border-red-700 bg-red-50 dark:bg-red-900/20 p-4">
                <div class="flex items-start gap-3">
                    <flux:icon.x-circle class="w-5 h-5 mt-0.5 shrink-0 text-red-600 dark:text-red-400" />
                    <div class="min-w-0 flex-1">
                        <p class="text-sm font-medium text-red-800 dark:text-red-200">
                            {{ trans_choice(':count submission was rejected|:count submissions were rejected', $rejectedSubmissions->count(), ['count' => $rejectedSubmissions->count()]) }}
                        </p>
                        <ul class="mt-1 space-y-1">
                            @foreach ($rejectedSubmissions as $rej)
                                <li class="text-xs text-red-700 dark:text-red-300">
                                    <span class="capitalize">{{ str_replace('_', ' ', $rej->action_type) }}</span>
                                    @if ($rej->rejection_reason)
                                        &mdash; "{{ Str::limit($rej->rejection_reason, 80) }}"
                                    @endif
                                    <span class="text-red-500 dark:text-red-400">({{ $rej->created_at->diffForHumans() }})</span>
                                </li>
                            @endforeach
                        </ul>
                        <flux:link href="{{ route('teacher.submissions.index') }}" class="mt-2 inline-block text-xs font-medium text-red-800 dark:text-red-200 underline" wire:navigate>
                            {{ __('View submissions') }} &rarr;
                        </flux:link>
                    </div>
                </div>
            </section>
        @endif

        {{-- ── Stats Cards ────────────────────────────────────────── --}}
        <section aria-label="{{ __('Teaching statistics') }}">
            <div class="grid grid-cols-2 gap-4 lg:grid-cols-4">
                <div class="p-4 rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800">
                    <div class="flex items-center gap-3">
                        <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-blue-100 dark:bg-blue-900/30">
                            <flux:icon.academic-cap class="w-5 h-5 text-blue-600 dark:text-blue-400" />
                        </div>
                        <div>
                            <flux:text class="text-sm text-zinc-500 dark:text-zinc-400">{{ __('My Students') }}</flux:text>
                            <p class="text-2xl font-bold text-zinc-900 dark:text-white">{{ number_format($totalStudents) }}</p>
                        </div>
                    </div>
                    <p class="mt-2 text-xs text-zinc-500 dark:text-zinc-400">
                        {{ trans_choice(':count class|:count classes', $assignedClasses->count(), ['count' => $assignedClasses->count()]) }}
                    </p>
                </div>

                <div class="p-4 rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800">
                    <div class="flex items-center gap-3">
                        <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-emerald-100 dark:bg-emerald-900/30">
                            <flux:icon.document-text class="w-5 h-5 text-emerald-600 dark:text-emerald-400" />
                        </div>
                        <div>
                            <flux:text class="text-sm text-zinc-500 dark:text-zinc-400">{{ __('Results') }}</flux:text>
                            <p class="text-2xl font-bold text-zinc-900 dark:text-white">{{ number_format($totalResults) }}</p>
                        </div>
                    </div>
                </div>

                <div class="p-4 rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800">
                    <div class="flex items-center gap-3">
                        <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-purple-100 dark:bg-purple-900/30">
                            <flux:icon.clipboard-document-list class="w-5 h-5 text-purple-600 dark:text-purple-400" />
                        </div>
                        <div>
                            <flux:text class="text-sm text-zinc-500 dark:text-zinc-400">{{ __('Assignments') }}</flux:text>
                            <p class="text-2xl font-bold text-zinc-900 dark:text-white">{{ number_format($totalAssignments) }}</p>
                        </div>
                    </div>
                </div>

                <div class="p-4 rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800">
                    <div class="flex items-center gap-3">
                        <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-amber-100 dark:bg-amber-900/30">
                            <flux:icon.clock class="w-5 h-5 text-amber-600 dark:text-amber-400" />
                        </div>
                        <div>
                            <flux:text class="text-sm text-zinc-500 dark:text-zinc-400">{{ __('Pending') }}</flux:text>
                            <p class="text-2xl font-bold text-zinc-900 dark:text-white">{{ number_format($pendingCount) }}</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        {{-- ── Quiz/Game Stats + AI Credits ───────────────────────── --}}
        <section aria-label="{{ __('Quiz, game and AI credit stats') }}">
            <div class="grid grid-cols-2 gap-4 lg:grid-cols-4">
                <div class="p-4 rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800">
                    <div class="flex items-center gap-3">
                        <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-cyan-100 dark:bg-cyan-900/30">
                            <flux:icon.academic-cap class="w-5 h-5 text-cyan-600 dark:text-cyan-400" />
                        </div>
                        <div>
                            <flux:text class="text-sm text-zinc-500 dark:text-zinc-400">{{ __('Quizzes') }}</flux:text>
                            <p class="text-2xl font-bold text-zinc-900 dark:text-white">{{ number_format($publishedQuizzes) }}</p>
                        </div>
                    </div>
                    <p class="mt-2 text-xs text-zinc-500 dark:text-zinc-400">{{ __(':count attempts', ['count' => number_format($quizAttempts)]) }}</p>
                </div>

                <div class="p-4 rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800">
                    <div class="flex items-center gap-3">
                        <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-pink-100 dark:bg-pink-900/30">
                            <flux:icon.puzzle-piece class="w-5 h-5 text-pink-600 dark:text-pink-400" />
                        </div>
                        <div>
                            <flux:text class="text-sm text-zinc-500 dark:text-zinc-400">{{ __('Games') }}</flux:text>
                            <p class="text-2xl font-bold text-zinc-900 dark:text-white">{{ number_format($publishedGames) }}</p>
                        </div>
                    </div>
                    <p class="mt-2 text-xs text-zinc-500 dark:text-zinc-400">{{ __('Published') }}</p>
                </div>

                <div class="p-4 rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800">
                    <div class="flex items-center gap-3">
                        <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-teal-100 dark:bg-teal-900/30">
                            <flux:icon.chart-bar class="w-5 h-5 text-teal-600 dark:text-teal-400" />
                        </div>
                        <div>
                            <flux:text class="text-sm text-zinc-500 dark:text-zinc-400">{{ __('Avg Score') }}</flux:text>
                            <p class="text-2xl font-bold text-zinc-900 dark:text-white">{{ $avgQuizScore !== null ? number_format((float) $avgQuizScore, 0) . '%' : '—' }}</p>
                        </div>
                    </div>
                    <p class="mt-2 text-xs text-zinc-500 dark:text-zinc-400">{{ __('Quiz average') }}</p>
                </div>

                <div class="p-4 rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800">
                    <div class="flex items-center gap-3">
                        <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-indigo-100 dark:bg-indigo-900/30">
                            <flux:icon.sparkles class="w-5 h-5 text-indigo-600 dark:text-indigo-400" />
                        </div>
                        <div>
                            <flux:text class="text-sm text-zinc-500 dark:text-zinc-400">{{ __('AI Credits') }}</flux:text>
                            <p class="text-2xl font-bold text-zinc-900 dark:text-white">{{ number_format($aiCreditsRemaining) }}</p>
                        </div>
                    </div>
                    <p class="mt-2 text-xs text-zinc-500 dark:text-zinc-400">{{ $aiCreditsLabel }}</p>
                </div>
            </div>
        </section>

        {{-- ── Results Progress + Assignments Coverage ────────────── --}}
        @if ($resultsProgress->isNotEmpty() || $assignmentsCoverage->isNotEmpty())
            <div class="grid gap-6 lg:grid-cols-2">
                {{-- Results Upload Progress per Class --}}
                @if ($resultsProgress->isNotEmpty())
                    <section aria-labelledby="results-progress-heading" class="rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800 p-4">
                        <flux:heading size="sm" id="results-progress-heading" class="mb-3">{{ __('Results Upload Progress') }}</flux:heading>
                        <p class="text-xs text-zinc-500 dark:text-zinc-400 mb-3">{{ __('Approved results per class this term') }}</p>
                        <div class="space-y-3">
                            @foreach ($resultsProgress as $rp)
                                @php $rpPercent = $rp['total'] > 0 ? round(($rp['uploaded'] / $rp['total']) * 100) : 0; @endphp
                                <div>
                                    <div class="flex items-center justify-between text-sm mb-1">
                                        <span class="text-zinc-700 dark:text-zinc-300 truncate">{{ $rp['name'] }}</span>
                                        <span class="font-medium text-zinc-900 dark:text-white shrink-0 ml-2">{{ $rp['uploaded'] }}/{{ $rp['total'] }}</span>
                                    </div>
                                    <div class="w-full h-2 rounded-full bg-zinc-200 dark:bg-zinc-700 overflow-hidden" role="progressbar" aria-valuenow="{{ $rpPercent }}" aria-valuemin="0" aria-valuemax="100" aria-label="{{ $rp['name'] }} {{ __('results progress') }}">
                                        <div class="h-full rounded-full transition-all duration-300 {{ $rpPercent >= 80 ? 'bg-emerald-500' : ($rpPercent >= 50 ? 'bg-amber-500' : 'bg-red-500') }}" style="width: {{ $rpPercent }}%"></div>
                                    </div>
                                </div>
                            @endforeach
                        </div>
                    </section>
                @endif

                {{-- Assignments Coverage per Class --}}
                @if ($assignmentsCoverage->isNotEmpty())
                    <section aria-labelledby="assignments-coverage-heading" class="rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800 p-4">
                        <flux:heading size="sm" id="assignments-coverage-heading" class="mb-3">{{ __('Assignments Coverage') }}</flux:heading>
                        <p class="text-xs text-zinc-500 dark:text-zinc-400 mb-3">{{ __('Weeks uploaded per class (:total weeks/term)', ['total' => $weeksPerTerm]) }}</p>
                        <div class="space-y-3">
                            @foreach ($assignmentsCoverage as $ac)
                                @php $acPercent = $ac['total'] > 0 ? round(($ac['uploaded'] / $ac['total']) * 100) : 0; @endphp
                                <div>
                                    <div class="flex items-center justify-between text-sm mb-1">
                                        <span class="text-zinc-700 dark:text-zinc-300 truncate">{{ $ac['name'] }}</span>
                                        <span class="font-medium text-zinc-900 dark:text-white shrink-0 ml-2">{{ $ac['uploaded'] }}/{{ $ac['total'] }}</span>
                                    </div>
                                    <div class="w-full h-2 rounded-full bg-zinc-200 dark:bg-zinc-700 overflow-hidden" role="progressbar" aria-valuenow="{{ $acPercent }}" aria-valuemin="0" aria-valuemax="100" aria-label="{{ $ac['name'] }} {{ __('assignment coverage') }}">
                                        <div class="h-full rounded-full transition-all duration-300 {{ $acPercent >= 80 ? 'bg-emerald-500' : ($acPercent >= 50 ? 'bg-amber-500' : 'bg-red-500') }}" style="width: {{ $acPercent }}%"></div>
                                    </div>
                                </div>
                            @endforeach
                        </div>
                    </section>
                @endif
            </div>
        @endif

        {{-- ── Upcoming Deadlines + My Classes ────────────────────── --}}
        <div class="grid gap-6 lg:grid-cols-2">
            {{-- Upcoming Due Dates --}}
            <section aria-labelledby="upcoming-deadlines-heading" class="rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800">
                <div class="p-4 border-b border-zinc-200 dark:border-zinc-700">
                    <flux:heading size="sm" id="upcoming-deadlines-heading">{{ __('Upcoming Deadlines') }}</flux:heading>
                </div>
                <div class="divide-y divide-zinc-200 dark:divide-zinc-700">
                    @forelse ($upcomingDeadlines as $deadline)
                        @php
                            $daysLeft = now()->diffInDays($deadline->due_date, false);
                            $urgent = $daysLeft <= 2;
                        @endphp
                        <div class="flex items-center gap-3 p-4">
                            <div class="flex items-center justify-center w-8 h-8 rounded-full shrink-0 {{ $urgent ? 'bg-red-100 dark:bg-red-900/30' : 'bg-blue-100 dark:bg-blue-900/30' }}">
                                <flux:icon.calendar class="w-4 h-4 {{ $urgent ? 'text-red-600 dark:text-red-400' : 'text-blue-600 dark:text-blue-400' }}" />
                            </div>
                            <div class="min-w-0 flex-1">
                                <p class="text-sm font-medium text-zinc-900 dark:text-white truncate">
                                    {{ $deadline->title ?? __('Week :week', ['week' => $deadline->week_number]) }}
                                </p>
                                <p class="text-xs text-zinc-500 dark:text-zinc-400">{{ $deadline->class?->name }}</p>
                            </div>
                            <div class="text-right shrink-0">
                                <p class="text-sm font-medium {{ $urgent ? 'text-red-600 dark:text-red-400' : 'text-zinc-700 dark:text-zinc-300' }}">
                                    {{ $deadline->due_date->format('M j') }}
                                </p>
                                <p class="text-xs {{ $urgent ? 'text-red-500 dark:text-red-400' : 'text-zinc-500 dark:text-zinc-400' }}">
                                    @if ($daysLeft == 0)
                                        {{ __('Today') }}
                                    @elseif ($daysLeft == 1)
                                        {{ __('Tomorrow') }}
                                    @else
                                        {{ __(':days days', ['days' => $daysLeft]) }}
                                    @endif
                                </p>
                            </div>
                        </div>
                    @empty
                        <div class="p-8 text-center">
                            <flux:icon.calendar class="w-8 h-8 mx-auto text-zinc-400" />
                            <flux:text class="mt-2 text-sm text-zinc-500 dark:text-zinc-400">
                                {{ __('No upcoming deadlines') }}
                            </flux:text>
                        </div>
                    @endforelse
                </div>
            </section>

            {{-- Assigned Classes --}}
            <section aria-labelledby="my-classes-heading" class="rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800">
                <div class="p-4 border-b border-zinc-200 dark:border-zinc-700">
                    <flux:heading size="sm" id="my-classes-heading">{{ __('My Classes') }}</flux:heading>
                </div>
                <div class="divide-y divide-zinc-200 dark:divide-zinc-700">
                    @forelse ($assignedClasses as $class)
                        <div class="flex items-center justify-between p-4">
                            <div class="min-w-0 flex-1">
                                <p class="font-medium text-zinc-900 dark:text-white">{{ $class->name }}</p>
                                <flux:text class="text-sm text-zinc-500 dark:text-zinc-400">
                                    {{ $class->level?->name }}
                                </flux:text>
                            </div>
                            <div class="flex items-center gap-3">
                                <flux:badge size="sm" color="zinc">
                                    {{ trans_choice(':count student|:count students', $class->students_count, ['count' => $class->students_count]) }}
                                </flux:badge>
                                <flux:button variant="subtle" size="xs" icon="eye" href="{{ route('teacher.students.index', ['class_id' => $class->id]) }}" wire:navigate>
                                    <span class="sr-only">{{ __('View students') }}</span>
                                </flux:button>
                            </div>
                        </div>
                    @empty
                        <div class="p-8 text-center">
                            <flux:icon.building-library class="w-8 h-8 mx-auto text-zinc-400" />
                            <flux:text class="mt-2 text-sm text-zinc-500 dark:text-zinc-400">
                                {{ __('No classes assigned yet. Ask your school admin to assign you to a class.') }}
                            </flux:text>
                        </div>
                    @endforelse
                </div>
            </section>
        </div>

        {{-- ── Recent Submissions ─────────────────────────────────── --}}
        <section aria-labelledby="recent-submissions-heading" class="rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800">
            <div class="flex items-center justify-between p-4 border-b border-zinc-200 dark:border-zinc-700">
                <div class="flex items-center gap-2">
                    <flux:heading size="sm" id="recent-submissions-heading">{{ __('Recent Submissions') }}</flux:heading>
                    @if ($pendingCount > 0)
                        <flux:badge color="amber" size="sm">{{ $pendingCount }} {{ __('pending') }}</flux:badge>
                    @endif
                </div>
                <flux:link href="{{ route('teacher.submissions.index') }}" class="text-sm" wire:navigate>
                    {{ __('View all') }}
                </flux:link>
            </div>
            <div class="divide-y divide-zinc-200 dark:divide-zinc-700">
                @forelse ($recentSubmissions as $submission)
                    <div class="flex items-start gap-3 p-4">
                        <div class="flex items-center justify-center w-8 h-8 mt-0.5 rounded-full shrink-0
                            @if ($submission->status === 'approved') bg-emerald-100 dark:bg-emerald-900/30
                            @elseif ($submission->status === 'pending') bg-amber-100 dark:bg-amber-900/30
                            @else bg-red-100 dark:bg-red-900/30 @endif">
                            @if ($submission->status === 'approved')
                                <flux:icon.check class="w-4 h-4 text-emerald-600 dark:text-emerald-400" />
                            @elseif ($submission->status === 'pending')
                                <flux:icon.clock class="w-4 h-4 text-amber-600 dark:text-amber-400" />
                            @else
                                <flux:icon.x-mark class="w-4 h-4 text-red-600 dark:text-red-400" />
                            @endif
                        </div>
                        <div class="min-w-0 flex-1">
                            <flux:text class="text-sm">
                                <span class="capitalize">{{ str_replace('_', ' ', $submission->action_type) }}</span>
                                &mdash;
                                @if ($submission->status === 'approved')
                                    <flux:badge color="green" size="sm" class="ml-1">{{ __('Approved') }}</flux:badge>
                                @elseif ($submission->status === 'pending')
                                    <flux:badge color="yellow" size="sm" class="ml-1">{{ __('Pending') }}</flux:badge>
                                @else
                                    <flux:badge color="red" size="sm" class="ml-1">{{ __('Rejected') }}</flux:badge>
                                @endif
                            </flux:text>
                            <flux:text class="text-xs text-zinc-500 dark:text-zinc-400">
                                {{ $submission->created_at->diffForHumans() }}
                                @if ($submission->status === 'rejected' && $submission->rejection_reason)
                                    &mdash; {{ Str::limit($submission->rejection_reason, 60) }}
                                @endif
                            </flux:text>
                        </div>
                    </div>
                @empty
                    <div class="p-8 text-center">
                        <flux:icon.inbox class="w-8 h-8 mx-auto text-zinc-400" />
                        <flux:text class="mt-2 text-sm text-zinc-500 dark:text-zinc-400">
                            {{ __('No submissions yet. Upload a result, assignment, or post a notice to get started.') }}
                        </flux:text>
                    </div>
                @endforelse
            </div>
        </section>

        {{-- ── Quick Actions ──────────────────────────────────────── --}}
        @if ($assignedClasses->isNotEmpty())
            <section aria-labelledby="quick-actions-heading" class="rounded-lg border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-800 p-4">
                <flux:heading size="sm" id="quick-actions-heading" class="mb-4">{{ __('Quick Actions') }}</flux:heading>
                <div class="grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-4">
                    <flux:button variant="filled" size="sm" icon="arrow-up-tray" href="{{ route('teacher.results.create') }}" wire:navigate class="justify-center">
                        {{ __('Upload Result') }}
                    </flux:button>
                    <flux:button variant="filled" size="sm" icon="clipboard-document-list" href="{{ route('teacher.assignments.create') }}" wire:navigate class="justify-center">
                        {{ __('Upload Assignment') }}
                    </flux:button>
                    <flux:button variant="filled" size="sm" icon="megaphone" href="{{ route('teacher.notices.create') }}" wire:navigate class="justify-center">
                        {{ __('Post Notice') }}
                    </flux:button>
                    <flux:button variant="filled" size="sm" icon="academic-cap" href="{{ route('teacher.quizzes.create') }}" wire:navigate class="justify-center">
                        {{ __('Create Quiz') }}
                    </flux:button>
                    <flux:button variant="filled" size="sm" icon="puzzle-piece" href="{{ route('teacher.games.create') }}" wire:navigate class="justify-center">
                        {{ __('Create Game') }}
                    </flux:button>
                </div>
            </section>
        @endif
    </div>
</x-layouts::app>
